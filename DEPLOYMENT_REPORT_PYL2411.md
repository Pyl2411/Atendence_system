# Deployment Report

Project repo for `Pyl2411` account:

`https://github.com/Pyl2411/Atendence_system`

Recommended Streamlit Cloud settings:

- Repository: `Pyl2411/Atendence_system`
- Branch: `main`
- Main file path: `app.py`

Notes:

- `app.py` is the entrypoint for Streamlit Cloud.
- `streamlit_app.py` contains the app logic.
- `requirements.txt` is the cloud-safe dependency file.
- `requirements-desktop.txt` is for local OpenCV use on PC.

Local run:

```powershell
cd "C:\\attendence system"
.\start_streamlit.ps1
```
